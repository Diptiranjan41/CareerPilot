server.port=8080

# ========== MySQL Database Configuration ==========
spring.datasource.url=jdbc:mysql://localhost:3306/careerpilot_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool
spring.datasource.hikari.maximumPoolSize=10
spring.datasource.hikari.minimumIdle=5

# ========== JWT Configuration ==========
jwt.secret=careerPilotSecretKey2024ForJWTToken123456789
jwt.expiration=86400000

# ========== Google OAuth2 Configuration ==========

google.client.id=your_id
google.client.secret=your_secret

# ========== Email Configuration for OTP ==========
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mahapatradiptiranjan064@gmail.com
spring.mail.password=jnjz deel sysv kyql
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ========== OTP Configuration ==========
otp.expiration.minutes=10
otp.length=6

# ========== AI Study Planner Configuration ==========
# Mock Test Settings
mock-test.default-questions=30
mock-test.default-time-minutes=30
mock-test.difficulty-levels=EASY,MEDIUM,HARD

# Study Plan Settings
study-plan.default-duration-weeks=8
study-plan.daily-goal-questions=20
study-plan.weekly-mock-tests=2

# AI Recommendations
ai.recommendations.daily=3
ai.recommendations.weekly=5
ai.refresh-interval-hours=24

# Leaderboard Settings
leaderboard.top-count=100
leaderboard.refresh-interval-minutes=60

# XP and Streak Settings
xp.per-question=10
xp.per-mock-test=50
streak-bonus-multiplier=1.5

# ========== Certificate Configuration ==========
certificate.qr-size=150
certificate.verification-url=https://ai-career-2sbc.vercel.app/verify-certificate
certificate.expiry-days=365
certificate.max-storage-size=5MB

# ========== File Upload Configuration ==========
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========== CORS Configuration ==========
cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://ai-career-2sbc.vercel.app
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# ========== Session Configuration ==========
server.servlet.session.timeout=86400
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
server.servlet.session.cookie.same-site=lax

# ========== Logging ==========
logging.level.com.careerpilot=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ========== Application Info ==========
app.name=CareerPilot AI
app.version=1.0.0
app.description=AI-Powered Career Guidance Platform

# ========== Verification Configuration ==========
verification.base-url=https://ai-career-2sbc.vercel.app/
verification.qr-code.size=120
verification.enable-qr=true